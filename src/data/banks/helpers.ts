import { makeQuestion } from "@/data/make-question";

/** Phase I Paper 2 (IT). */
export const p1p2 = makeQuestion(1, 2);

/** Phase II Paper 2 (IT dry-run). */
export const p2p2 = makeQuestion(2, 2);

/** Phase I Paper 1 (GA / English / Quant / Reasoning). */
export const p1p1 = makeQuestion(1, 1);
